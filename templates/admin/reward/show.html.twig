{% extends 'base.html.twig' %}

{% block title %}Reward{% endblock %}

{% block body %}
<section class="bloc-full">
    <aside id="column-left" class="bloc-column-abs">
        <h1 class="bloc-title"></h1>
        <div class="bloc-link-column">
            <ul>
                <li></li>
            </ul>
        </div>
    </aside>
    <section class="bloc-large-abs">
        <h2 class="bloc-title"></h2>
    </section>
</section>
    <h1>Reward</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reward.id }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ reward.createdAt ? reward.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ reward.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reward.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_reward_index') }}">back to list</a>

    <a href="{{ path('app_admin_reward_edit', {'id': reward.id}) }}">edit</a>

    {{ include('admin/reward/_delete_form.html.twig') }}
{% endblock %}
