{% extends 'base.html.twig' %}

{% block title %}Modifier catégorie{% endblock %}

{% block body %}
<div id="arrow-left" class="arrow" href="">{{ include('svg/arrowleft.html.twig') }}</div>

<section class="bloc-full">
    <aside id="column-left" class="bloc-column-abs">
        <h1 class="bloc-title"></h1>
        <div class="bloc-link-column">
            <ul>
                <li>
                    {{ include('admin/category/_delete_form.html.twig') }}
                </li>
                <li>
                    <a class="btmain" href="{{ path('app_admin_category_index') }}">Retour</a>
                </li>
            </ul>
        </div>
    </aside>
    <section class="bloc-large-abs">
        <h2 class="bloc-title"></h2>
        <div class="form-base1">
            {{ include('admin/category/_form.html.twig', {'button_label': 'Mise à jour'}) }}
        </div>
    </section>
</section>
{% endblock %}
