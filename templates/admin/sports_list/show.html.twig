{% extends 'base.html.twig' %}

{% block title %}SportsList{% endblock %}

{% block body %}
<div id="arrow-left" class="arrow" href="">{{ include('svg/arrowleft.html.twig') }}</div>

<section class="bloc-full">
    <aside id="column-left" class="bloc-column-abs">
        <h1 class="bloc-title"></h1>
        <div class="bloc-link-column">
            <ul>
                <li></li>
            </ul>
        </div>
    </aside>
    <section class="bloc-large-abs">
        <h2 class="bloc-title"></h2>
    </section>
</section>
    <h1>SportsList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sports_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sports_list.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_sports_list_index') }}">back to list</a>

    <a href="{{ path('app_admin_sports_list_edit', {'id': sports_list.id}) }}">edit</a>

    {{ include('admin/sports_list/_delete_form.html.twig') }}
{% endblock %}
