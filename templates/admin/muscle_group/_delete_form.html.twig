<form method="post" action="{{ path('app_admin_muscle_group_delete', {'id': muscle_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ muscle_group.id) }}">
    <button class="btn">Delete</button>
</form>
