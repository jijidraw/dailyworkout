{% extends 'base.html.twig' %}

{% block title %}Equipment{% endblock %}

{% block body %}
<section class="bloc-full">
    <aside id="column-left" class="bloc-column-abs">
        <h1 class="bloc-title"></h1>
        <div class="bloc-link-column">
            <ul>
                <li></li>
            </ul>
        </div>
    </aside>
    <section class="bloc-large-abs">
        <h2 class="bloc-title"></h2>
    </section>
</section>
    <h1>Equipment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipment.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_equipment_index') }}">back to list</a>

    <a href="{{ path('app_admin_equipment_edit', {'id': equipment.id}) }}">edit</a>

    {{ include('admin/equipment/_delete_form.html.twig') }}
{% endblock %}
