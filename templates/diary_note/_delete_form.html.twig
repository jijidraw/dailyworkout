<form method="post" action="{{ path('app_diary_note_delete', {'id': diary_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diary_note.id) }}">
    <button class="btdelete">Effacer</button>
</form>
