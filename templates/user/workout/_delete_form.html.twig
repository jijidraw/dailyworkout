<form method="post" action="{{ path('app_workout_delete', {'id': workout.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce workout ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workout.id) }}">
    <input class="btblack" type="submit" value="Supprimer le workout">
</form>
