<div class="form-comment hidden" id="post-comment{{ publi.id }}">
    <form method="POST" id="comment-form" action="{{ path('newcomment', {id: publi.id })}}" data-id="{{ publi.id }}" class="js-comment">
        <input class="input-comment" type="textarea" id="texte" placeholder="Entrez votre texte" >
    
        <a href="{{ path('newcomment', {id: publi.id })}}" id="path" class="hidden">lien</a>
        <input class="btmain" type="submit" id="submit">
    </form>
</div>